/**
 * Escreva uma função que remova todas as duplicatas de um array ordenado, de modo que 
 * cada elemento apareça apenas uma vez. A função deve retornar o novo comprimento do 
 * array sem as duplicatas.
 */
